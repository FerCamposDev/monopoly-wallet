import { ListItem, ListItemText } from "@mui/material";

const SaveGameItem = () => {

  return (
    <ListItem onClick={undefined}>
      <ListItemText>
        Save Game
      </ListItemText>
    </ListItem>
  )
}

export default SaveGameItem